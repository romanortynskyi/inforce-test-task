import { Errors as ProductErrors } from './products/product-errors';
import { Errors as CommentErrors } from './comments/comment-errors';

export const Errors = {
    PRODUCTS: ProductErrors,
    COMMENTS: CommentErrors,
};
